[project]
name = "chaos-proxy"
version = "0.1.0"
description = "Эмулятор сетевых задержек и потерь пакетов (Network Chaos Monkey)"
authors = [{ name = "Your Name", email = "your@email.com" }]
dependencies = [
    "typer>=0.9.0",
]

[project.scripts]
chaos-proxy = "chaos_proxy.cli.app:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"